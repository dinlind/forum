{% set passwordResetLink = url('password_reset', { 'confirmationToken': user.confirmationToken }) %}

Dear {{ user.username }},

Click the link bellow to reset your password.

{{ passwordResetLink }}

Best regards,
The forum team
