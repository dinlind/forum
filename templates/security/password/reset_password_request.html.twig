{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-center vertical-center centered-container-wrapper">
        <div class="centered-container">
            {{ form_start(form) }}
                {{ form_row(form.email) }}

                <button class="btn btn-md btn-primary" type="submit">
                    Send reset link
                </button>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
